# Small Large Equal
入力された2つの数値の大小を出力します。

# Requirement
* Rust

# Usage
```bash
cargo run -p small-large-equal -- 1 2
// a < b
cargo run -p small-large-equal -- 2 1
// a > b
cargo run -p small-large-equal -- 1 1
// a == b
```